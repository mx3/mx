<%#= controller.class.name.to_s.html_safe -%>

<% content_for :center do -%>
  <!-- Sandy - custom sites will have their own folders in public/site/FOO (were foo = test for sample development purposes)
             We might consider a app/views/site/test for custom pages as well. 
             We'll change "test" to #{proj.unix_name} for multiple sites in the example below.  
  -->
  <%= render(:partial => "public/site/test/trait_index") %>
<% end %>

<%= render :partial => 'layout' -%>
