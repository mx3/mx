<div>
  <% if @part == 'center' %>
    <ul>
      <% for chr in @chrs %>
        <li><%= link_to(chr.name, :controller => :chrs, :action => :show, :id => chr)  -%></li>
      <% end %>
    </ul>
  <% end %>
</div>
