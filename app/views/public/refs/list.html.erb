<% unless @ref_pages.first == @ref_pages.last -%>  <strong>View page <%= pagination_links @ref_pages, :window_size => 3, :sort => @sort %></strong><% end -%><%= render :collection => @refs, :partial => 'r' %><% unless @ref_pages.first == @ref_pages.last %>  <strong>View page <%= pagination_links @ref_pages, :window_size => 3, :sort => @sort %></strong><% end %>