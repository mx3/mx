<%# We might need a public paginator. %>
<%= paginate(@refs, :total_count => @refs.total_count) %>
<%= render :collection => @refs, :partial => 'r' %>
<%= paginate(@refs, :total_count => @refs.total_count) %>

