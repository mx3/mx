<h3>Listing Tags</h3>
<div id='search_results'>
  <%= paginate(@tags, :total_count => @tags.total_count) -%>
  <%= render(:partial => 'record_table') -%>
  <%= paginate(@tags, :total_count => @tags.total_count) -%>
</div>
