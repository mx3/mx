<!-- "Should be using new shared/_picker partial!" --!>
<%= render(:partial=>'shared/picker', :locals=> {
      :controller=>'ref',
      :object=>object,
      :method=>method,
      :tag_id=>tag_id,
      :size  =>size,
      :display=>display
   })
%>
