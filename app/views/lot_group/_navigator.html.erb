<div class='navigator'>
  <%= navigator(@lot_group,  session['lot_group_view'], "id") -%>

  <%# the links to various views, should likely show them versus hidden tabs or not !! -%>
  <div class="item"> <%= session['lot_group_view'] == 'show' ? '<span class="navigator_current">default</span>' :  link_to('default', :action => 'show', :id => @lot_group)  %> </div>  
  <div class="item"> <%= session['lot_group_view'] == 'show_members' ? '<span class="navigator_current">members</span>' :  link_to('members', :action => 'show_members', :id => @lot_group)  %> </div>  
  
  <br />
</div>
