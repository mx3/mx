<h3>New label</h3>

<%= form_for(:label, :url => {:action => :create}) do |f| %>
   <%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

