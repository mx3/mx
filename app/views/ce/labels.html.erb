<p> <i> Experiment with lines per column and columns/page to get the best fit for your labels, the default should work well with insect labels.  You will also want to resize your browser window to get the best fit.  To queue labels to print edit a given label and enter a number  > 0 in number to print.</i></p>

<div class="subform">
  <%= form_tag :action => 'labels_clear_to_zero' do -%>
  Clear all labels to print to zero. <%= submit_tag("Clear", {:confirm => "Are you sure you want to reset all labels to print to zero?"})  %>
  <% end -%>
</div>

<div class="subform">
  <%= form_tag :action => 'labels_print_preview' do -%>
  Label type: <%= select('lbl', 'type', Ce::LABEL_PRINT_STYLES.keys.collect{|k| [ Ce::LABEL_PRINT_STYLES[k], k]} ) %> 
  Lines per column: <%= select('lbl', 'lines_per_col', [200, 10,20, 25, 30, 35, 40, 50, 60,80,100,120, 160, 180, 220, 240]) %> 
  Columns per page: <%= select('lbl', 'columns_max', [14, 13, 12, 11, 10, 9, 8, 7 ,6,5,4]) %> 
  Print preview. <%= submit_tag "Preview" %>
  <% end -%>
</div>

<h3>Labels to print</h3>

<table>
  <tr>
    <th>Label</th><th>#</th>
  </tr>
  <% for l in @labels %>
  <tr>
    <td><%= l.print_label %></td><td><%= l.num_to_print %></td><td><%= link_to('edit', :action => 'edit', :id => l) %></td>
  </tr>
  <% end %>
</table>
